---
name: web-fetch
description: 公開URLを取得し、HTMLをMarkdown全文に変換する。
---

# Web Fetch

`web-fetch --help` で入力形式と終了コードを確認します。

`web-fetch <url>` を実行し、必要なら標準出力をファイルに保存します。出力は要約されていない原文です。

取得先が許可されていない場合は、CLIの診断を提示します。
