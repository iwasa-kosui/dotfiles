---
name: spec
description: |
  RDRA 3.0に基づく要件定義と仕様駆動開発を支援するスキル。
  5つのモード（分析/仕様作成/実装生成/レビュー/更新）で要件の構造化からコード生成までを一貫して実行する。
  RDRA、要件定義、仕様書作成、SDD、ビジネスユースケース、ユースケース、業務フロー、
  情報モデル、状態遷移、トレーサビリティ、タスク分解、コード生成、受け入れテストなど、
  要件定義・仕様策定・実装生成に関する相談全般でトリガーする。
  要件定義したい、仕様書を書きたい、仕様をレビューしたい、仕様を更新したい、
  タスクに分解したい、コードを生成したいといった漠然とした依頼でも積極的にトリガーすること。
---

# Spec

RDRA 3.0に基づく要件定義から仕様駆動開発まで一貫して支援するスキル。RDRA成果物はYAMLフロントマター付きMarkdownで管理し、`traces_to` による単方向トレーサビリティでGOALから実装タスクまでのWhy依存チェーンを形成する。

## モード選択

ユーザーの意図に応じてモードを判定する。明示的な指定がなければ、現在の成果物の有無を確認して提案する。

| ユーザーの意図 | モード |
|--------------|--------|
| 要件を整理したい、RDRAで分析したい | 分析 |
| 仕様書を書きたい、PRD/ADR/Specが欲しい | 仕様作成 |
| タスクに分解したい、コードを生成したい、テストを書きたい | 実装生成 |
| 仕様をレビューしたい、抜け漏れを確認したい | レビュー |
| 要件が変わった、仕様を更新したい | 更新 |

成果物がない状態で仕様作成を求められたら、先に分析モードの実行を提案する。

## 出力ディレクトリ構造

```
{project}/
├── rdra/                          # RDRA成果物
│   ├── overview.md                # 全体概観（アクター・ゴール・コンテキスト一覧）
│   ├── contexts/                  # コンテキスト別バーティカルスライス
│   │   └── {context-name}.md      # 4レイヤー縦貫（価値/環境/境界/システム）
│   ├── shared/                    # コンテキスト横断モデル
│   │   ├── information-model.md   # エンティティ定義 + ER図
│   │   └── state-models.md        # 状態モデル定義 + 状態遷移図
│   └── traceability.yaml          # 全要素のトレーサビリティ
├── specs/                         # SDD文書
│   ├── prd.md                     # 要件定義書
│   ├── adr/                       # 設計判断記録
│   │   └── NNN-*.md
│   ├── spec.md                    # 技術仕様書
│   └── tasks.md                   # タスクリスト
└── change-log.md                  # 変更履歴（更新モード）
```

## IDスキーマ

要素種別ベースのセマンティックIDを使い、`traces_to` で上位要素を参照する。

| レイヤー | プレフィックス | 対象 |
|---------|---------------|------|
| システム価値 | `ACTOR-*`, `GOAL-*`, `REQ-*` | アクター、ゴール、要求 |
| 外部環境 | `BIZ-*`, `BUC-*` | コンテキスト、ビジネスUC |
| システム境界 | `UC-*`, `SCR-*`, `EVT-*` | ユースケース、画面、イベント |
| システム | `INFO-*`, `STATE-*`, `COND-*`, `VAR-*` | 情報、状態、条件、バリエーション |
| SDD | `FR-*`, `SPEC-*`, `TASK-*` | 機能要件、仕様、タスク |

スキーマ詳細は `references/rdra-schemas.md` を参照。

## トレーサビリティ

`traces_to` のみで単方向のWhy依存チェーンを形成する。`traced_from` は保持せず、必要時に逆引きで算出する。

```
GOAL ← REQ ← BUC ← UC ← FR ← SPEC ← TASK
```

全要素を `traceability.yaml` に集約し、孤立要素（GOALに到達しないチェーン）をゼロに保つ。

---

## モード1: 分析（RDRA Analysis）

ビジネス要件をRDRA 3.0の4レイヤーで構造化する。コンテキスト単位のバーティカルスライスで対話的にヒアリングし、YAML + Mermaid図を生成する。

詳細な進め方は `references/analysis-guide.md` を参照。

### フロー概要

```
Phase 1: スコープ把握 → overview.md ドラフト
Phase 2: コンテキスト特定 → コンテキスト一覧を合意
Phase 3: コンテキスト別深掘り（繰り返し）
  → §価値: ゴール・要求
  → §環境: BUC・業務フロー
  → §境界: UC・画面・イベント
  → §システム: 情報・状態・条件・バリエーションへの参照
  → contexts/{name}.md 生成
Phase 4: 横断モデル整理 → shared/*.md 生成
Phase 5: トレーサビリティ生成 → traceability.yaml
```

### ヒアリング原則

- 1フェーズにつき1テーマに集中する。複数レイヤーを同時に聞かない
- 曖昧な回答には具体例を示して選択肢を提示する
- 各コンテキスト完了時にMermaid図を提示し認識合わせする
- 「他に〜はありますか？」で網羅性を確認する

### Mermaid図

図テンプレートは `references/mermaid-templates.md` を参照。以下の7種を使い分ける:

| RDRAモデル | Mermaid図種 |
|-----------|-------------|
| システムコンテキスト | `graph TB` |
| ビジネスコンテキスト | `graph LR` |
| ビジネスユースケース | `graph TB` |
| 業務フロー | `sequenceDiagram` |
| ロバストネス図 | `flowchart` + `classDef` |
| 情報モデル | `erDiagram` |
| 状態モデル | `stateDiagram-v2` |

YAMLがSSoT。Mermaid図はYAMLから生成されるビューとして扱い、YAML更新時はMermaid図も必ず再生成する。

---

## モード2: 仕様作成（Spec Writing）

RDRA成果物を入力とし、PRD/ADR/Specの3文書を生成する。テンプレートは `references/sdd-templates.md` を参照。

### 5 Step フロー

1. **RDRA成果物の読み込みと検証** — 構造的不備があれば分析モードへの差し戻しを提案
2. **PRD生成** — ゴール・要求→背景と課題、BUC→ユーザーストーリー、UC→機能要件にマッピング
3. **ADR生成** — 設計判断ポイントをADR候補として提示し、対話で判断理由を明確化
4. **Spec生成** — UC・画面・イベントから技術仕様（API設計、データモデル、状態遷移、エラーハンドリング）を生成
5. **トレーサビリティ更新** — RDRA要素 → PRD → Spec の `traces_to` チェーンを構築し、カバレッジレポートを出力

各文書内でRDRA要素IDを参照し、トレーサビリティを維持する。

---

## モード3: 実装生成（Implementation Generation）

仕様書を入力とし、タスクリスト・実装コード・ADR・受け入れテストを生成する。SDDの「Specify → Plan → Tasks → Implement」ワークフローをRDRAトレーサビリティ付きで実行する。

詳細は `references/implementation-guide.md` を参照。

### 4 Step フロー

1. **技術スタック・アーキテクチャのヒアリング** — 言語・フレームワーク・インフラ・チーム慣習を把握
2. **タスク分解** — Specの各仕様項目をタスクに分解。各タスクにTASK-NNN ID、`traces_to`、受け入れ条件、依存関係を付与。`tasks.md` を生成
3. **実装・ADR・テスト生成** — タスク単位で実装コード、技術判断ADR、BUC/UCベースの受け入れテストを生成
4. **トレーサビリティ更新・検証** — GOAL → TASK のチェーン完結を検証し、カバレッジレポートを出力

生成コードはあくまで「たたき台」。人間によるレビューを前提とする。

---

## モード4: レビュー（Spec Review）

Agent toolでサブエージェントを起動し、`agents/reviewer.md` の手順に従ってレビューを実行する。メインエージェントのコンテキストから分離することで、生成時のバイアスに影響されにくいレビューを実現する。

### 起動方法

```
Agent tool:
  prompt: |
    agents/reviewer.md を読み込み、その手順に従ってレビューを実行してください。
    RDRA成果物パス: {rdra/の絶対パス}
    SDD文書パス: {specs/の絶対パス}
    レビュー対象: all
```

### 4つのレビュー観点

1. **網羅性** — RDRA要素がSDD文書に漏れなく反映されているか
2. **トレーサビリティ** — `traces_to` チェーンがGOALまで到達するか、孤立要素はないか
3. **曖昧性** — 「適切に」「必要に応じて」等の曖昧表現を検出し、具体化を提案
4. **整合性** — RDRA成果物内・RDRA-SDD間の整合性、ADR決定のSpec反映

---

## モード5: 更新（Spec Update）

要件変更時に影響範囲を分析し、関連する成果物を更新する。

### 4 Step フロー

1. **影響分析** — 変更対象の `traces_to` を逆引きし、影響を受ける要素をツリーで可視化
2. **計画提示** — 更新が必要なファイルと変更内容を表形式で提示し、ユーザー承認を得る
3. **更新実行** — RDRA成果物のYAML更新 + Mermaid図再生成、SDD文書修正、traceability.yaml更新、change-log.md追記
4. **再検証** — レビューモード（トレーサビリティ + 整合性）で更新後の不整合を検出

YAML変更を含む更新では、対象ファイルのMermaid図を必ずYAMLから再生成する。
