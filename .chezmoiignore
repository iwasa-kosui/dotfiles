# Repository management files (should not be deployed to $HOME)
Brewfile
# ルートのCLAUDE.mdのみ無視（.claude/CLAUDE.mdはchezmoiで管理する）
CLAUDE.md
!.claude/CLAUDE.md
README.md
docs/
tests/
LICENSE

# ローカル専用ルール（~/.claude/rules/ に手動配置。chezmoi 管理外）
dot_claude/rules/local.md
dot_cursor/rules/local.mdc

# Neovim starter template remnants
dot_config/nvim/LICENSE
dot_config/nvim/README.md

# textlint は install-claude-textlint フックが bun install で復元する。
.claude/node_modules

# rpt dependencies are restored by the install hook and build caches regenerate locally.
.local/lib/rpt/node_modules
.local/lib/rpt/template/.astro
.local/lib/rpt/template/.astro-cache
.local/lib/rpt/template/.vite-cache

# web-fetch dependencies are restored by `bun install` and regenerate locally.
.local/lib/web-fetch/node_modules

# macOS artifacts
**/.DS_Store

# macOS 専用の LaunchAgent は他 OS へ配置しない
{{ if ne .chezmoi.os "darwin" -}}
Library/LaunchAgents/me.kosui.ollama-keep-alive.plist
{{ end -}}
