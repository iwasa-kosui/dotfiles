# rpt Skill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Codex、Claude、Cursorへ共通の`rpt`スキルを配布し、材料整理から安全なMDX、単一HTML、任意のモバイルプレビューまでを実行できるようにする。

**Architecture:** スキル本文は手順と判断境界だけを持ち、実行時に引数なしの`rpt`を呼んで最新authoring contractを取得する。3環境の`SKILL.md`はbyte-identicalにし、Codexだけ`agents/openai.yaml`を持つ。deployed buildはAstro CLIをBun interpreter経由で起動し、package fileの実行bitへ依存しない。

**Tech Stack:** Agent Skills (`SKILL.md`, `agents/openai.yaml`)、Chezmoi、Bun、TypeScript、Astro、`rpt` CLI

## Global Constraints

- skill名は`rpt`、配置先は`dot_codex/skills/rpt`、`dot_claude/skills/rpt`、`dot_cursor/skills/rpt`とする。
- 3環境の`SKILL.md`はbyte-identicalとし、frontmatterは`name`と`description`だけを持つ。
- descriptionは`Use when...`で始め、レポート、報告書、HTML、MDX、`rpt`、モバイルプレビューのtriggerを含め、workflowを要約しない。
- 構文契約をスキルへ複製せず、毎回引数なしの`rpt` stdoutを正本として読む。
- skill固有のscript、reference、assetは追加しない。
- モバイル配信は生成HTMLだけを置いた専用directoryに限定し、`/private/tmp`やrepository全体を配信しない。
- build validationを別表記で迂回せず、exit 3は入力修正、exit 4は`--debug`による原因診断へ分岐する。
- 新規skillはbaseline REDを確認してから作成し、同じscenarioでGREENを確認する。
- `.Codex/`とAstro/Vite cacheはstageまたはcommitしない。

---

### Task 1: Skill baseline RED

**Files:**
- Create outside git: `.superpowers/rpt-skill-eval/baseline.md`

**Interfaces:**
- Consumes: skillを含まないfresh agent、worktree、次の固定scenario。
- Produces: raw response、artifact paths、実行command、失敗criteriaを記録したbaseline report。Task 2のSKILL.mdはこのreportで観測した欠落だけを補う。

- [ ] **Step 1: baseline scenario用の材料を作る**

`/private/tmp/rpt-skill-baseline-input.md`へ次を保存する。これはagentへ渡す唯一の業務材料であり、skill pathや期待workflowを含めない。

```markdown
# Weekly release facts

- Audience: engineering managers
- Status: release candidate is ready
- Tests: 201 passed, 0 failed
- Risk: Mermaid requires a pinned CDN; normal reports have no client JavaScript
- Milestones: validation completed, static components completed, security review completed
- Ask: create a concise HTML report and make it viewable on a phone
```

- [ ] **Step 2: skillなしfresh agentを実行する**

controllerは`fork_turns="none"`でfresh subagentを起動し、次だけを渡す。

```text
/private/tmp/rpt-skill-baseline-input.md の内容から、マネージャ向けの簡潔なHTMLレポートを作り、編集可能なsourceとスマホで開けるURLも返してください。確認質問は不要です。成果物は /private/tmp/rpt-skill-baseline-output/ 配下だけに作成してください。
```

skill一覧、`rpt`、期待command、失敗criteriaをpromptへ追加しない。

- [ ] **Step 3: baselineを失敗判定する**

次の各項目をraw responseとfilesystemで判定する。

```text
contract: 引数なしrptを実行したか
source: 編集可能な.mdxを返したか
build: rpt buildで単一HTMLを生成したか
safety: 禁止構文を使わずbuildがexit 0か
mobile: HTMLだけの専用directoryを0.0.0.0で配信したか
handoff: HTML、MDX、http URLを返したか
```

1件以上の欠落をREDとする。すべて満たした場合はskillで改善すべき自然な失敗がないため、Task 2へ進まずユーザーへ報告する。

- [ ] **Step 4: baseline reportを書く**

`.superpowers/rpt-skill-eval/baseline.md`へprompt、agent status、raw response、各criteriaのPASS/FAIL、観測した誤りをそのまま記録する。repository fileは変更せず、Task 1はcommitしない。

評価後、preview serverを起動していた場合はPIDまたはsessionを記録して停止し、`/private/tmp/rpt-skill-baseline-output/`を削除する。Task 4で同じ入力を使うため、`/private/tmp/rpt-skill-baseline-input.md`は残す。

---

### Task 2: Cross-runtime rpt skill

**Files:**
- Create: `tests/rpt-skill.test.ts`
- Create: `dot_codex/skills/rpt/SKILL.md`
- Create: `dot_codex/skills/rpt/agents/openai.yaml`
- Create: `dot_claude/skills/rpt/SKILL.md`
- Create: `dot_cursor/skills/rpt/SKILL.md`

**Interfaces:**
- Consumes: Task 1の`.superpowers/rpt-skill-eval/baseline.md`。
- Produces: `rpt` skill、Codex UI metadata、3環境同一性を検証するBun test。

- [ ] **Step 1: skill contractの失敗testを書く**

`tests/rpt-skill.test.ts`へ次を追加する。

```ts
import { expect, test } from "bun:test";
import { join } from "node:path";

const root = join(import.meta.dir, "..");
const skillPaths = [
  "dot_codex/skills/rpt/SKILL.md",
  "dot_claude/skills/rpt/SKILL.md",
  "dot_cursor/skills/rpt/SKILL.md",
].map((path) => join(root, path));

test("rpt skill is identical across Codex Claude and Cursor", async () => {
  const contents = await Promise.all(skillPaths.map((path) => Bun.file(path).text()));
  expect(contents[1]).toBe(contents[0]);
  expect(contents[2]).toBe(contents[0]);
});

test("rpt skill discovers report creation and uses the live CLI contract", async () => {
  const skill = await Bun.file(skillPaths[0]).text();
  expect(skill).toMatch(/^---\nname: rpt\ndescription: Use when /);
  expect(skill).toContain("レポート");
  expect(skill).toContain("HTML");
  expect(skill).toContain("MDX");
  expect(skill).toContain("mobile-preview-url");
  expect(skill).toContain("Run `rpt` without arguments");
  expect(skill).toContain("Treat stdout as the authoritative authoring contract");
  expect(skill).toContain("rpt build");
  expect(skill).toContain("exit code 3");
  expect(skill).toContain("exit code 4");
  expect(skill).toContain("If `rpt` is unavailable");
  expect(skill).toContain("If the authoring contract cannot be read");
  expect(skill).toContain("dedicated preview directory");
});

test("Codex rpt skill exposes matching UI metadata", async () => {
  const metadata = await Bun.file(
    join(root, "dot_codex/skills/rpt/agents/openai.yaml"),
  ).text();
  expect(metadata).toContain('display_name: "rpt Reports"');
  expect(metadata).toContain('short_description: "安全な単一HTMLレポートを作成してモバイルプレビュー"');
  expect(metadata).toContain("$rpt");
});
```

- [ ] **Step 2: focused testをREDにする**

Run: `bun test tests/rpt-skill.test.ts`

Expected: missing `SKILL.md`でFAIL。

- [ ] **Step 3: canonical skillをinitializeする**

Run:

```bash
python3 /Users/kosui/.codex/skills/.system/skill-creator/scripts/init_skill.py rpt \
  --path dot_codex/skills \
  --interface 'display_name=rpt Reports' \
  --interface 'short_description=安全な単一HTMLレポートを作成してモバイルプレビュー' \
  --interface 'default_prompt=$rptを使って、この内容から安全な単一HTMLレポートを作成してください。'
```

Expected: `dot_codex/skills/rpt/SKILL.md`と`agents/openai.yaml`が生成される。resourcesとexamplesは生成しない。

- [ ] **Step 4: baseline failureだけを補うminimal SKILL.mdを書く**

`SKILL.md`は500 words未満、imperative formで次の順序にする。

```markdown
---
name: rpt
description: Use when the user asks to create, update, build, or preview a report, 報告書, HTML report, or MDX report with rpt, including requests to make results viewable on a phone.
---

# rpt Reports

Create a safe, editable MDX source and build one shareable HTML report.

## Workflow

1. Extract purpose, audience, decision, source material, and output location. Ask one question only when a missing answer materially changes the report; otherwise make a stated assumption.
2. Gather only the requested evidence. Browse when freshness, citations, or the request requires it.
3. Run `rpt` without arguments. Treat stdout as the authoritative authoring contract. Do not rely on remembered component or HTML syntax.
4. Write the editable `.mdx` source. Use rich components only when they improve comprehension.
5. Run `rpt build <input.mdx> -o <output.html>`. For exit code 3, fix the reported input location without weakening the content. For exit code 4, rerun with `--debug` and diagnose the runtime; never bypass validation.
6. Verify both files exist, then return clickable absolute paths for the HTML and MDX. Mention the pinned-CDN requirement when Mermaid is present.

## Error handling

If `rpt` is unavailable, report that installation or `chezmoi apply` is required. If the authoring contract cannot be read, stop instead of guessing MDX syntax. Fix input diagnostics directly; never disguise forbidden scripts, expressions, attributes, URLs, or CSS.

## Mobile preview

Copy only the generated HTML to `index.html` inside a dedicated preview directory. Serve that directory on `0.0.0.0`, then run `mobile-preview-url <port>`. Never serve `/private/tmp`, a repository, a home directory, or a directory containing unrelated files.

If the phone sends TLS bytes to the HTTP server, provide the Meshnet IP URL with an explicit `http://` scheme.

## Handoff

Return the report title, HTML path, MDX path, source caveats, and preview URL when requested. Keep the server running until the user is done.
```

Task 1で別の具体的failureが観測された場合だけ、そのfailureを防ぐ1文を該当sectionへ追加する。構文一覧は追加しない。

- [ ] **Step 5: 3環境へ同一skillを配置する**

`dot_codex/skills/rpt/SKILL.md`の完成内容を、byteを変えずに次へ複製する。

```text
dot_claude/skills/rpt/SKILL.md
dot_cursor/skills/rpt/SKILL.md
```

- [ ] **Step 6: focused testとskill validatorをGREENにする**

Run: `bun test tests/rpt-skill.test.ts`

Run:

```bash
python3 /Users/kosui/.codex/skills/.system/skill-creator/scripts/quick_validate.py dot_codex/skills/rpt
python3 /Users/kosui/.codex/skills/.system/skill-creator/scripts/quick_validate.py dot_claude/skills/rpt
python3 /Users/kosui/.codex/skills/.system/skill-creator/scripts/quick_validate.py dot_cursor/skills/rpt
```

Expected: testは3 pass、validatorは3環境とも`Skill is valid!`。

- [ ] **Step 7: Task 2をcommitする**

```bash
git add tests/rpt-skill.test.ts dot_codex/skills/rpt dot_claude/skills/rpt dot_cursor/skills/rpt
git commit -m "feat(rpt): レポート作成スキルを3環境へ追加" -m "Co-Authored-By: Codex GPT-5 <noreply@openai.com>"
```

---

### Task 3: Astro build without executable package files

**Files:**
- Create: `dot_local/lib/rpt/src/astro-command.ts`
- Create: `tests/rpt-astro-command.test.ts`
- Modify: `dot_local/lib/rpt/src/build.ts:65-90`

**Interfaces:**
- Consumes: `packageRoot: string`。
- Produces: `createAstroBuildCommand(packageRoot: string): readonly ["bun", string, "build"]`。`buildReport`はこのcommandを`Bun.spawn`へ渡す。

- [ ] **Step 1: non-executable script用の失敗testを書く**

`tests/rpt-astro-command.test.ts`へ次を追加する。

```ts
import { expect, test } from "bun:test";
import { chmod, mkdir, mkdtemp, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { createAstroBuildCommand } from "../dot_local/lib/rpt/src/astro-command.ts";

test("Astro build command runs a package script without its executable bit", async () => {
  const root = await mkdtemp(join(tmpdir(), "rpt-astro-command-"));
  const script = join(root, "node_modules/astro/astro.js");
  try {
    await mkdir(join(root, "node_modules/astro"), { recursive: true });
    await writeFile(script, 'console.log(process.argv[2]);\n');
    await chmod(script, 0o644);

    const process = Bun.spawn([...createAstroBuildCommand(root)], {
      stdout: "pipe",
      stderr: "pipe",
    });
    expect(await process.exited).toBe(0);
    expect(await new Response(process.stdout).text()).toBe("build\n");
    expect(await new Response(process.stderr).text()).toBe("");
  } finally {
    await rm(root, { recursive: true, force: true });
  }
});
```

- [ ] **Step 2: focused testをmodule missingでREDにする**

Run: `bun test tests/rpt-astro-command.test.ts`

Expected: `Cannot find module '../dot_local/lib/rpt/src/astro-command.ts'`。

- [ ] **Step 3: Bun interpreter commandを実装する**

`dot_local/lib/rpt/src/astro-command.ts`:

```ts
import { join } from "node:path";

export function createAstroBuildCommand(
  packageRoot: string,
): readonly ["bun", string, "build"] {
  return ["bun", join(packageRoot, "node_modules/astro/astro.js"), "build"];
}
```

`build.ts`で`createAstroBuildCommand`をimportし、現在の`["bun", "run", "--cwd", temporaryRoot, "astro", "build"]`を次へ置換する。

```ts
const process = Bun.spawn([...createAstroBuildCommand(packageRoot)], {
  cwd: temporaryRoot,
  stdout: "pipe",
  stderr: "pipe",
});
```

- [ ] **Step 4: focused testとrpt E2EをGREENにする**

Run: `bun test tests/rpt-astro-command.test.ts`

Expected: 1 pass、0 fail。

Run: `bun test tests/rpt.e2e.test.ts --timeout 120000`

Expected: all pass、0 fail。

- [ ] **Step 5: 型検査を実行する**

Run: `bun run --cwd dot_local/lib/rpt check`

Expected: 0 errors、0 warnings、0 hints。

- [ ] **Step 6: Task 3をcommitする**

```bash
git add dot_local/lib/rpt/src/astro-command.ts dot_local/lib/rpt/src/build.ts tests/rpt-astro-command.test.ts
git commit -m "fix(rpt): Astro buildを実行bit非依存にする" -m "Co-Authored-By: Codex GPT-5 <noreply@openai.com>"
```

---

### Task 4: Deploy and forward-test the skill

**Files:**
- Update outside git: `.superpowers/rpt-skill-eval/forward.md`
- Deploy: `~/.codex/skills/rpt/`, `~/.claude/skills/rpt/`, `~/.cursor/skills/rpt/`
- Deploy: `~/.local/bin/rpt`, `~/.local/lib/rpt/`
- Update: draft PR #139

**Interfaces:**
- Consumes: Task 1 baseline scenario、Task 2 skill path、Task 3 deployed CLI fix。
- Produces: artifact-backed GREEN evaluation、deployed skill/CLI、updated draft PR。

- [ ] **Step 1: source treeをfresh verificationする**

Run:

```bash
bun test tests/rpt-skill.test.ts tests/rpt-astro-command.test.ts tests/rpt.e2e.test.ts tests/rpt-safe-style.test.ts --timeout 120000
bun run --cwd dot_local/lib/rpt check
git diff --check
```

Expected: all tests pass、checkは0 diagnostics、diff checkは出力なし。

- [ ] **Step 2: chezmoi差分を対象限定で確認する**

Run:

```bash
chezmoi diff -S "/Users/kosui/.local/share/chezmoi/.wt/codex/feat-rpt-ai-native-help" \
  "/Users/kosui/.codex/skills/rpt" \
  "/Users/kosui/.claude/skills/rpt" \
  "/Users/kosui/.cursor/skills/rpt" \
  "/Users/kosui/.local/bin/rpt" \
  "/Users/kosui/.local/lib/rpt"
```

Expected: rpt skillとTask 3のrpt runtime差分だけを表示する。

- [ ] **Step 3: 5 targetへchezmoi applyする**

Run: Step 2の`chezmoi diff`を`chezmoi apply`へ置換した同一target command。

Expected: exit 0。

- [ ] **Step 4: deployed CLIを実buildでsmoke testする**

`/private/tmp/rpt-skill-deployed-smoke.mdx`:

```mdx
---
title: Deployed rpt smoke
status: final
---

<Status tone="success">deployed build works</Status>
```

まずsource treeのCLIで同じfixtureをbuildする。

Run:

```bash
bun dot_local/bin/executable_rpt build \
  /private/tmp/rpt-skill-deployed-smoke.mdx \
  -o /private/tmp/rpt-skill-source-smoke.html --force
```

次にdeployed CLIでbuildする。

Run:

```bash
/Users/kosui/.local/bin/rpt build \
  /private/tmp/rpt-skill-deployed-smoke.mdx \
  -o /private/tmp/rpt-skill-deployed-smoke.html --force
cmp /private/tmp/rpt-skill-source-smoke.html /private/tmp/rpt-skill-deployed-smoke.html
```

Expected: 両buildがexit 0、stdoutは各HTMLのabsolute path、stderrなし。HTMLに`data-rpt-component="status"`を含み、`cmp`もexit 0。

- [ ] **Step 5: skill付きfresh agentでGREENを確認する**

controllerはTask 1と同じモデル、`fork_turns="none"`でfresh subagentを起動し、次だけを渡す。

```text
Use $rpt at /Users/kosui/.local/share/chezmoi/.wt/codex/feat-rpt-ai-native-help/dot_codex/skills/rpt/SKILL.md. /private/tmp/rpt-skill-baseline-input.md の内容から、マネージャ向けの簡潔なHTMLレポートを作り、編集可能なsourceとスマホで開けるURLも返してください。確認質問は不要です。成果物は /private/tmp/rpt-skill-forward-output/ 配下だけに作成してください。
```

Task 1の6 criteriaを同じ方法で採点する。HTML/MDXの存在、`rpt build` exit 0、専用preview directory、http URLをfilesystemとserver logで確認する。

- [ ] **Step 6: GREEN evaluationを記録する**

`.superpowers/rpt-skill-eval/forward.md`へprompt、agent status、raw response、6 criteria、baselineとの差を記録する。必須criteriaにFAILがあればskillへ最小修正を加え、3環境へ同じ変更を反映し、Task 2のtest/validatorと同じscenarioを再実行する。

評価後、preview serverを停止し、`/private/tmp/rpt-skill-forward-output/`を削除する。再現に必要なhash、command、server logはevaluation reportへ残す。

- [ ] **Step 7: branchをpushしてPR #139を更新する**

Run: `git push origin codex/feat-rpt-ai-native-help`

PR titleは`feat(rpt): AI向けリッチコンテンツと作成スキルを追加`へ更新する。本文の「内容」へ動的help参照型skillと3環境配布を加え、「論点」へskillが構文を複製しない判断を加える。既存のMermaid nonce-only tradeoffとTest Planは維持する。

Run: `gh pr view 139 --json url,isDraft,title,headRefName,baseRefName,commits`

Expected: draft、head=`codex/feat-rpt-ai-native-help`、base=`main`、Task 2とTask 3のcommitを含む。

- [ ] **Step 8: 最終状態を確認する**

Run:

```bash
git status --short --branch
chezmoi diff -S "/Users/kosui/.local/share/chezmoi/.wt/codex/feat-rpt-ai-native-help" \
  "/Users/kosui/.codex/skills/rpt" \
  "/Users/kosui/.claude/skills/rpt" \
  "/Users/kosui/.cursor/skills/rpt" \
  "/Users/kosui/.local/bin/rpt" \
  "/Users/kosui/.local/lib/rpt"
```

Expected: upstreamとHEADが一致し、既知の`.Codex/`とAstro/Vite cache以外の未追跡fileがなく、対象chezmoi diffは空。
